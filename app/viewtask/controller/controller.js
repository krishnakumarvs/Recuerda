(function() {
    'use strict';

    /**
     * Get the main module (shared for Workout).
     */
    angular.module(appName)
    /**
     * Login Controller.
     */
    .controller('ViewTaskController', ViewTask);

    ViewTask.$inject = ['$state'];

    function ViewTask($state) {
        var loginVm = this;
        
        
                  
        
    }

})();